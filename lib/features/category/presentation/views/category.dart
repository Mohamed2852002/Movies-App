import 'package:flutter/cupertino.dart';

class Category extends StatelessWidget {
  String title ;
  var imagename ;

  Category({ required this.title , required this.imagename });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
